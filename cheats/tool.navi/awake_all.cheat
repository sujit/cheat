% builtins,all,awake

# "all" built-in function usages
  * (all ["Written", "Created"] (in activity.smb.file.actions))
  * (all ["Backup", "Awake/Backup_Servers", "Veeam"] (\x -> x in device.tags))
  * (all config.networks.eduAndResearch (contains activity.ip.source))